# Changelog

Quota Desk 的版本更新记录。

## [0.3.15] - 2026-09-15

### 新增

- 账号「用量」统计页（DeepSeek / Z.ai / Codex / MiniMax）：近一年逐日用量热力图，点击方格查看当日明细；头部汇总卡片显示累计消耗、峰值与连续使用天数；入口常驻，未登录时在页面内直接引导登录，登录成功原地刷新。
- Z.ai 官方用量：复用账号 API Key 直读智谱开放平台；接入 credit-usage/activity 云端统计（与 ZCode「个人套餐」页同一份数据），累计 Token、峰值日、当前/最长连续天数全量对齐，热力图覆盖完整一年，近 60 天由 model-usage 补充逐模型明细。
- DeepSeek 官方用量：浏览器登录官方账号后读取逐日花费与 Token；MiniMax 官方用量：浏览器登录开放平台读取每日账单历史；Codex 官方用量：复用本机 Codex CLI 的 ChatGPT 登录读取逐日 Token 统计，凭据全程不暴露给界面。
- 连续天数兜底：厂商接口不提供连续天数时，按逐日数据本地计算当前连续与最长连续（当天还没有用量时从昨天起算）。

### 改进

- 热力图着色改为固定阈值分级（Token：<1000万 / 1000-3000万 / 3000-8000万 / >8000万；金额：<¥30 / ¥30-100 / ¥100-250 / >¥250），颜色深度反映绝对用量，重度使用日的波动清晰可辨。
- 统计卡片数值不再重复单位（标题已带 Token），大数以万 / 亿紧凑显示，悬停可见完整数字。

## [0.3.14] - 2026-09-14

### 新增

- 总览卡片拖拽排序：按住卡片空白处拖动即可改序，短按仍进详情，文字上拖动仍可复制；总览按用户顺序展示，巡检成功或失败只更新状态，不再把异常账号挤到最后。
- 趋势图时间范围切换：悬停条右侧可切「全部 / 1 天 / 7 天 / 1 个月」，默认 1 天，历史不够长的档位置灰不可选；左右平移仍属同一时长时保持档位，滚轮缩放对不上任何档则取消高亮；悬停明细收成 5h / 1h47m / 2d 的紧凑格式。

### 改进

- 纯百分比窗口不再把 0/0 或 limit=100 的占位数字写进历史与浪费档案，浪费明细不再出现虚假的 0/0 剩余量。

### 修复

- 浪费统计移除「幽灵周期」：旧版算法的突升兜底曾给同一次重置重复归档「提前重置 + 自然到期」两条档案（如 WLB 09/06→09/07 与 08/31→09/07 实为同一次自然重置），导致图表多出一根柱子、周期数虚高。现在启动时全量清洗档案文件，读取与归档时同样过滤；判据为两条档案共享同一观测点，真实的提前重置不受影响。
- Kimi 订阅月度额度口径修正：改用会员池整体用量 `amountUsedRatio` 计算剩余（此前误用 coding 专属口径 `kimiCodeUsedRatio`，月度剩余显示偏多）；响应缺失该字段时才回退 coding 口径。

## [0.3.13] - 2026-09-13

### 新增

- 账号停用：不再续约的账号可「停用」而无需删除。停用后停止巡检、桌面浮窗不再显示，账号、额度历史、周期浪费档案与凭据全部原地保留，随时可重新启用并接续监控（启用后立即补一次巡检；周期归档按历史全量扫描，能识别停用空窗前后分属不同周期）。
- 总览视图底部以一行「N 个账号已停用，点击展开查看」收纳停用账号，展开后的档案式卡片只展示停用日期与停用时长（不渲染冻结的实时额度），点击进入历史页；行式/周期视图不显示停用账号；历史页头部在账号右侧标注「已停用 · 日期」；设置抽屉账号行的停用/启用开关为红色停止 / 绿色播放图标（停用账号灰显并隐藏手动刷新）。
- 应用内操作确认弹窗：停用/删除账号、清除历史的确认改为应用内弹窗（危险操作为红色按钮），不再调用系统级对话框。

### 改进

- 全部弹窗与空状态移除「小标签 + 主标题」的两层标题结构，只保留单个主标题。
- NSIS 安装配置增加 `createDesktopShortcut: "always"`：每次安装器运行（含 electron-updater 的静默更新）都确保桌面快捷方式存在，规避个别环境下更新后快捷方式缺失的问题。

### 修复

- 点击额度提醒的桌面通知无响应：现在会呼起并聚焦主窗口（NSIS 安装版生效；便携版受系统限制无点击激活）。

## [0.3.12] - 2026-09-13

### 新增

- 额度浪费统计：账号历史页新增「趋势 | 浪费」切换。每次轮询检测周/月额度的周期结束事件，把周期末剩余额度归档为周期档案（永久保留，不受历史保留时长影响）；浪费视图用柱状图展示每个周期的浪费率（每根柱 = 一个周期区间，最新在右），附平均线与「周期数 / 平均浪费率 / 累计浪费」摘要。
- 可信度分级：周期末最后一次记录距重置超过 2 倍轮询间隔（多为关机断档）时标记为「可能失真」，浪费值按上界展示且不计入平均；厂商提前重置（周期被截断清零）的周期单独标记，同样不计入平均。悬停柱子可查看周期区间、浪费率、剩余量与记录时间点，方便对账。
- 厂商级浪费窗口预设：只有固定周期型窗口参与统计（Kimi / Z.ai / WLB / MiniMax / Claude 默认仅 7 天，Codex / Kimi 订阅为 7 天 + 月度，余额与每日刷新类不参与）；厂商编辑弹窗可勾选覆盖，全部取消则该厂商不做浪费统计；滑动窗口自动排除。余额型账号的历史页不显示「浪费」入口。
- 历史保留时长新增「永久」档：超过 30 天的记录自动降采样为每小时一条，点数上限同步放宽。

### 改进

- 专属适配厂商（Claude / Codex / Gemini / Kimi 订阅 / Grok）的编辑表单精简为名称、Logo、官网与浪费统计窗口，不再展示内置接管的接口、认证与脚本配置，避免误改内置适配。
- 弹窗尺寸收敛：宽度统一 400px，高度与主窗口留白协调，不再近乎撑满整个窗口。
- Grok 额度窗口修正为仅 7 天（无月度限额）。
- 编辑账号不再允许修改 API 凭据（脚本厂商的内置 apiKey 变量同样锁定）：历史趋势与浪费档案都挂在账号上，换 key 即换账号，请删除后重新添加，避免新旧账号数据混在一起。
- 趋势 / 浪费两个视图锁定为同一高度，切换时卡片与切换按钮不再跳动，也不会顶出整页滚动条；滑动图表时不会再误选坐标轴文字。
- 浪费视图每根柱子下方直接标注周期区间（柱子过多时自动抽稀为结束日期），悬停另有完整提示；自然周期的展示起点修正为「结束时刻 − 周期长度」，不再把首次观测时间当成周期起点。
- 历史页底部的归档说明小字移除；记录数移到趋势视图图例行右下角，浪费视图不再显示；浪费视图当前周期文案精简为「剩余 X% 未用」，移除冗余的「已用 / 若现在重置」说明；无可靠周期时「累计」显示为 — 而不是 0.0 倍额度，累计数值保留两位小数。

### 修复

- 周期结束判定重写：以「resetAt 是否改期 + 旧重置时刻是否被两次轮询跨过」为准（被跨过 = 自然到期，未跨过被改期 = 提前重置），剩余率突升不再作为判据，resetAt 没变一律视为同周期；此前会漏掉「提前重置并把周期重排到一周后」这类 resetAt 变晚的场景。
- resetAt 暂时缺失的适配：记住最后一个带 resetAt 的快照，恢复后直接比对；直到历史末尾都没恢复则把消失本身认定为周期结束。
- 兼容厂商返回数值型（epoch 毫秒/秒）resetAt 的解析（此前 Z.ai 的周期完全无法归档）。

## [0.3.11] - 2026-09-12

### 新增

- Grok CLI 官方订阅接入统一认证路径（参考 cc-switch 的 xAI OAuth 实现）：按 auth.x.ai OIDC discovery 定位令牌端点，令牌临期或鉴权失败时自动用 refresh_token 续期并写回加密存储，不再依赖 grok CLI 自己刷新，解决导入后 token 容易过期的问题；本机 live 文件只合并原 scope 条目，绝不动其它 profile。
- Grok 账号令牌失效后可一键恢复：主界面异常账号的警告图标变为「重新导入」入口，重新收录本机登录即可恢复监控，账号名、标签与额度窗口配置全部保留。

### 改进

- 「添加账号」整体重构为统一渠道流程：打开后先进入渠道磁贴页（方形 Logo 按钮，三列居中布局；不可用渠道置灰，悬停说明原因），点击进入各渠道专属视图——CLI 渠道填写账号名与标签后导入、Kimi 订阅扫码登录；最后的「⋯」磁贴进入自定义 API / 中转接口表单（原「添加账号」表单）。所有视图在同一个固定尺寸窗口内切换，窗口尺寸不再随视图跳动。
- 原「导入订阅登录」入口移除，订阅渠道与自定义账号统一由「添加账号」进入；相关窗口标题随之理顺（添加账号 / 添加渠道 / 添加 Kimi 订阅 / 自定义账号）。

### 修复

- 并发续期去重：同一 Grok 账号同时触发手动刷新与定时轮询时共享一次续期，避免 xAI 轮换 refresh_token 后迟到的旧请求被判复用导致登录失效。
- 修复 gRPC-Web 以 HTTP 200 + trailer 返回鉴权错误（grpc-status 7/16）时不触发自动续期重试的问题。
- 设置抽屉账号行移除「本机激活」徽标，行高统一，不再出现个别行被撑高的情况。
## [0.3.10] - 2026-09-11

### 新增

- 新增「Kimi 订阅」官方订阅渠道：手机扫码登录（二维码在应用内生成，微信 / Kimi App 扫码确认），可同时监控 5 小时 / 7 天 / 月订阅三个额度窗口。月额度来自网页会员服务——CLI 侧接口对订阅用户不返回月度数据，因此本渠道凭据走网页会话（令牌只加密保存在本机），而非 kimi CLI 的 OAuth。
- 订阅令牌自动续期：access token 15 分钟过期，应用会用 refresh_token（90 天有效）自动续期并写回加密存储，无需重复扫码；国内 kimi.com 与海外 kimi.ai 双区自适应。
- token 失效后一键恢复：主界面异常账号的红色警告图标变为「重新扫码登录」入口（总览卡片与行式明细都有），重新扫码即可恢复监控，账号名、标签与额度窗口配置全部保留，也可顺手修改；扫到已属于其它账号的登录时会拒绝并提示换号。

### 改进

- 「导入订阅登录」弹窗整体重构：渠道列表与扫码页共用同一个固定尺寸窗口（切换视图尺寸不变、无滚动条），渠道改为等高卡片样式；Kimi 扫码分两步——先扫码验证，再设置账号名与标签，点「完成」才真正导入。
- 说明文字收进标题旁的问号图标：悬停显示，不再占据弹窗空间；修复悬停气泡被弹窗裁剪、以及它把弹窗撑出横向滚动条的问题。
- CLI 渠道行增加厂商 Logo 与精简文案（如「未登录 · ~/.codex/auth.json」），底部按钮统一为「退出」。

### 修复

- 修复 Kimi 5 小时额度用量为 0（剩余 100%）时窗口不显示的问题：官方接口按 proto3 规范省略零值字段，现按「字段缺失 = 0% 已用」解析，剩余额度与重置倒计时正常展示。

## [0.3.9] - 2026-09-10

### 新增

- CLI 官方订阅（Codex / Claude Code / Gemini CLI）多账号监控：每个账号可保存独立的登录快照（Windows DPAPI 加密），额度查询不再依赖本机 CLI 当前激活的登录——在 cc-switch 等工具里切换 profile 后，各账号额度照常刷新。设置 → 账号与凭据新增「导入本机 CLI 登录」：把当前本机登录一键收录为独立账号；想加第二个账号，在 CLI / cc-switch 里登录或切换过去后再回来导入一次即可（同一登录按账号指纹自动去重）。
- CLI 登录令牌自动续期：Codex（auth.openai.com）、Claude（console.anthropic.com）、Gemini（oauth2.googleapis.com）的账号快照会用各自的 refresh_token 在临期或 401 时自动续期并写回加密存储，后台账号不再因 access token 过期而失效。若该账号恰好是本机 CLI 当前激活的登录，续期结果还会原子写回 `~/.codex/auth.json` 等本机文件（写回前校验 refresh_token 未被切换工具改动，绝不覆盖 cc-switch 刚切换进去的 profile）。
- cc-switch 导入新增官方 OAuth 登录识别：Codex 官方登录条目（含完整 token bundle）现在也能导入为独立账号（此前只迁移 API Key），导入后同样自动续期。
- 设置抽屉账号行新增「本机激活」徽标：标出本机 CLI 当前使用的正是哪个账号；跟随本机登录的镜像账号标识会随切换自动更新。
- 「导入本机 CLI 登录」支持在导入前编辑账号信息：账号名默认就是渠道名（如 Codex，留空回退），标签可编辑（默认「日常」），标识自动取登录邮箱。
- 「添加账号」表单不再列出 Claude / Codex / Gemini 官方订阅，统一走「导入本机 CLI 登录」收录，避免两套入口混淆；表单底部提供「去导入」快捷入口。官方订阅账号的编辑表单同步隐藏「详细额度接口路径」与「新 API Token」字段（专属适配器不使用这两项）。

### 改进

- Codex 凭据路径遵循 `CODEX_HOME` 环境变量，与 codex CLI 自身行为一致。
- CLI 类账号的标识自动维护：优先显示登录邮箱（从官方 id_token 提取），用户手动填写的标识不会被覆盖。

## [0.3.8] - 2026-09-09

### 新增

- 设置抽屉新增「网络代理」：下拉选择「跟随系统」（默认，沿用系统代理）、「手动输入」代理地址（支持 `http://127.0.0.1:7897`、`socks5://127.0.0.1:7898`，只填 `host:port` 时按 http 代理处理）或「不使用代理」（全部直连）。所有账号的额度请求共用此代理，保存后立即生效，用于解决 Claude、Codex、Gemini、Grok 等境外厂商直连被中断的问题。
- 账号级请求超时：添加 / 编辑账号时可配置「请求超时（秒）」，范围 5–120、默认 15，跨境或代理网络下可调大；对所有适配方式（标准映射、脚本、CLI 凭据类专属适配）统一生效。
- 添加账号、编辑账号、更新凭据弹窗拆分为「测试」与「保存」两个按钮：「测试」按当前表单直接跑一次连通性检查（不保存，凭据 / 密钥留空自动回退已存值），结果内联显示在弹窗内；「保存」只保存、不再自动触发查询，保存等待时间可控。

### 改进

- 瞬时网络错误自动重试：连接超时、连接被中断 / 重置、DNS 抖动、响应被截断等会自动重试（共尝试 3 次，1 秒 / 2 秒退避），偶发网络抖动不再直接把账号标记为异常；凭据失效、HTTP 4xx/5xx 与业务失败不重试。
- 网络类报错翻译为可行动的中文提示：重试耗尽后按错误类型给出指引（调大账号超时、配置代理、检查网络等），不再把 `The operation was aborted due to timeout`、`net::ERR_CONNECTION_CLOSED` 这类原始英文错误直接展示给用户。

## [0.3.7] - 2026-09-05

### 修复

- 修复设置抽屉“账号与凭据”列表长文案把操作按钮挤出可视区的问题：账号行下方的说明（尤其是“凭据已失效，请在「设置 → 账号与凭据」中编辑该账号…”这类很长的报错）不再撑开整行，超出部分显示为省略号，鼠标悬停可查看完整内容；编辑 / 刷新 / 删除按钮和状态圆点在任何窗口宽度下都保持可见、可点击。

## [0.3.6] - 2026-08-27

### 新增

- 支持 wlbclub 上线的 1 天限额：`rate_limits` 中 `window=1d` 的行解析为新的“1 天”额度窗口；已经配置过的 wlbclub 账号升级后自动显示该窗口，无需重新编辑。“1 天”作为通用窗口类型加入默认窗口下拉、浮窗缩写（1d）、同心圆与明细排序、趋势图配色和托盘摘要。
- 额度趋势图在无数据时段（如程序关闭、刷新失败）不再强行连线：相邻两个采样点的间隔明显长于自身采样节奏（2 倍中位间隔，至少 10 分钟）时自动断开折线；鼠标悬停在断开区间上，详情栏显示该时段的时间范围并提示“该时段暂无数据”。

### 改进

- 趋势图纵轴随当前可见折线的实际范围自适应，不再固定 0–100%：例如只显示 30% 附近的 5 小时线时，纵轴收缩到对应区间，波动细节看得更清楚；通过图例隐藏 / 显示窗口时纵轴实时调整，百分比窗口的上下限始终夹在 0–100 之间。
- 轮询间隔默认从 15 分钟收紧为 5 分钟，可选项调整为 5 / 10 / 15 / 30 分钟（移除 1 小时）；旧配置中的 1 小时自动收敛为 30 分钟，主进程同样做了上限保护。

## [0.3.5] - 2026-08-26

### 修复

- 修复纯百分比额度窗口显示“已用 0.00 / 0.00”占位明细的问题：通用 JSON 适配器只配置百分比路径时不再生成假的总量 / 剩余数值，账号卡片与历史趋势图悬停详情在窗口没有真实数值时直接不显示该明细；已记录的历史数据中的 0 同样会被过滤。“已用 / 总量”明细现在要求窗口同时带有真实的剩余与总量数值，避免单位混用算出错误结果。
- 修复下载更新失败毫无反馈的问题：此前自动检查发现新版本后手动点“立即升级”，下载途中断网等失败会被当成后台网络异常静默吞掉，更新弹窗按钮直接消失。现在下载失败会明确提示原因，弹窗里的“重试”直接重新下载，而不是退回重新检查。
- 修复 Windows 便携版（portable）无法应用内升级却仍走下载流程的问题：现在与 macOS 一样，点“立即升级”直接打开 GitHub 发布页手动下载，更新弹窗的按钮与说明相应调整。
- 修复保存任意设置都会触发一次更新检查的问题：现在只有把“自动检查更新”从关闭切换为开启的那一刻才立即补一次检查，平时保存设置不再重复请求 GitHub。

### 改进

- 自动检查更新从“仅启动时检查一次”改为每小时定时检查一次（可在设置中关闭），常驻托盘不重启也不会错过新版本；后台检查只在空闲状态下发起，已有可用版本、下载中或已下载待安装时不重复打扰。
- 检查进行中重复触发不再叠加请求；启动自动检查尚未结束时手动点“检查更新”，会接管进行中的检查并把结果反馈给用户。

## [0.3.4] - 2026-08-25

### 修复

- 修复 Electron 43 下桌面浮窗置顶完全失效的问题：受上游回归影响，`alwaysOnTop` 构造参数与默认级别的 `setAlwaysOnTop` 不再真正生效，浮窗从创建起就没有置顶，会被任意窗口盖住。现在改为显式声明更高的置顶级别，并在浮窗显示、系统恢复可见、每次额度轮询后补一次置顶，其他置顶程序后来激活也不会再把浮窗压下去。
- 修复主窗口“固定在桌面最前面”不生效的问题（同一根因），取消固定同样正常工作。

### 改进

- 主窗口置顶或被激活时，桌面浮窗会自动回到主窗口上方，主界面再也不会挡住自己的浮窗。

## [0.3.3] - 2026-08-25

### 新增

- 额度历史趋势图：每次成功刷新额度都会记录一条快照，点击账号卡片（账号总览、行式明细、周期明细均可）进入该账号的折线图；横轴为时间、纵轴为剩余额度（百分比窗口取百分比，余额窗口取余额数值），每个额度窗口一条线。
- 折线图悬停任意数据点，图表顶部显示该点详情：时间、每条线的剩余额度、真实额度的“剩余 / 总量”数值（纯百分比窗口不显示无意义的 x/100），以及该点的重置倒计时。
- 折线图支持滚轮缩放时间范围：缩小一览全程，放大查看细节（最细到 10 分钟），放大后可拖动平移，可一键重置缩放；数据点稀疏时自动标出每个点。
- 图例可点击显示 / 隐藏单条折线（至少保留一条）。
- 设置新增“额度历史”区块：保留时长可选 3 天至 3 个月（默认 7 天），超期记录自动清理，支持一键清除全部历史；删除账号时连同该账号的历史一起删除。

### 改进

- 桌面浮窗默认大小与长度调整为 90%。
- 浮窗空间不足时标签先于账号名称被收起（标签 → 小圆点 → 隐藏），只有过长的名称才以省略号截断；主窗口行式 / 周期明细中同样让标签优先让位给名称。
- 浮窗尺寸变化后自适应布局会重新测量，修复间歇性出现“名称被省略但标签完整显示”的问题。

## [0.3.2] - 2026-08-25

### 新增

- 桌面浮窗支持单独调整长度（60%–150%），只改变横向宽度；浮窗默认宽度加宽，默认大小下完整展示账号的全部额度窗口（含 1 个月，缩写 1M），不做任何隐藏；缩短后空间不足时按“标签缩为小圆点 → 隐藏标签 → 逐个隐藏倒计时（最长周期优先）→ 收起最长周期额度”的顺序自动逐级收起，只有一个额度窗口的账号无需收起任何内容；名称放不下时以省略号截断，悬停查看完整内容。
- 桌面浮窗支持右键菜单，菜单内容与系统托盘右键菜单一致。

### 修复

- 修复桌面浮窗右键无反应的问题，并避免右键误触发拖动。
- 网络连接异常时，后台自动检查更新不再误报“更新失败”，静默等待下次检查。
- 设置页点击“检查更新”现在一定有反馈：检查中按钮显示状态，发现新版本自动弹出更新窗口，已是最新或网络异常时弹出提示。

## [0.3.1] - 2026-08-22

### 新增

- 新增 Grok、MiniMax Coding Plan、Claude Code、OpenAI Codex、Gemini CLI 的内置额度适配。
- 支持读取 Grok、Claude、Codex、Gemini 本机 CLI 的登录状态查询订阅额度，不需要在 Quota Desk 中重复填写凭据。
- 支持从本机 `~/.cc-switch/cc-switch.db` 导入 cc-switch 账号，可选择可识别的服务商，重复 API Key 自动去重，扫描过程中凭据只保留在主进程。
- 新增 New API 站点一键模板，包含接口地址、`accessToken` 和 `userId` 变量。
- 补齐内置服务商的官网入口和 Logo。
- 桌面浮窗支持 80%–300% 等比缩放，预览与实际缩放比例一致。

### 改进

- 优化服务商凭据过期或缺失时的错误提示，提供刷新或重新登录指引。
- 空状态页面增加直接导入 cc-switch 的入口，首次配置更顺畅。
- 更新弹窗将 Release HTML 说明转换为纯文本，并过滤自动生成的对比链接尾部。
- 增加 Grok 计费解析、cc-switch 凭据提取与服务商匹配、MiniMax 额度解析测试。
- 重写 README，补充产品定位、支持范围、安装说明，以及固定展示的亮色 / 暗色截图对照。

### 修复

- 修复开发模式下将 CommonJS 模板当作 ES Module 导入导致的白屏。
- 修复历史配置中的服务商 Logo 引用，并自动迁移到内置 Logo。
