# Changelog

Quota Desk 的版本更新记录。

## [0.3.3] - 2026-08-25

### 新增

- 额度历史趋势图：每次成功刷新额度都会记录一条快照，点击账号卡片（账号总览、行式明细、周期明细均可）进入该账号的折线图；横轴为时间、纵轴为剩余额度（百分比窗口取百分比，余额窗口取余额数值），每个额度窗口一条线。
- 折线图悬停任意数据点，图表顶部显示该点详情：时间、每条线的剩余额度、真实额度的“剩余 / 总量”数值（纯百分比窗口不显示无意义的 x/100），以及该点的重置倒计时。
- 折线图支持滚轮缩放时间范围：缩小一览全程，放大查看细节（最细到 10 分钟），放大后可拖动平移，可一键重置缩放；数据点稀疏时自动标出每个点。
- 图例可点击显示 / 隐藏单条折线（至少保留一条）。
- 设置新增“额度历史”区块：保留时长可选 3 天至 3 个月（默认 7 天），超期记录自动清理，支持一键清除全部历史；删除账号时连同该账号的历史一起删除。

### 改进

- 桌面浮窗默认大小与长度调整为 90%。
- 浮窗空间不足时标签先于账号名称被收起（标签 → 小圆点 → 隐藏），只有过长的名称才以省略号截断；主窗口行式 / 周期明细中同样让标签优先让位给名称。
- 浮窗尺寸变化后自适应布局会重新测量，修复间歇性出现“名称被省略但标签完整显示”的问题。

## [0.3.2] - 2026-08-25

### 新增

- 桌面浮窗支持单独调整长度（60%–150%），只改变横向宽度；浮窗默认宽度加宽，默认大小下完整展示账号的全部额度窗口（含 1 个月，缩写 1M），不做任何隐藏；缩短后空间不足时按“标签缩为小圆点 → 隐藏标签 → 逐个隐藏倒计时（最长周期优先）→ 收起最长周期额度”的顺序自动逐级收起，只有一个额度窗口的账号无需收起任何内容；名称放不下时以省略号截断，悬停查看完整内容。
- 桌面浮窗支持右键菜单，菜单内容与系统托盘右键菜单一致。

### 修复

- 修复桌面浮窗右键无反应的问题，并避免右键误触发拖动。
- 网络连接异常时，后台自动检查更新不再误报“更新失败”，静默等待下次检查。
- 设置页点击“检查更新”现在一定有反馈：检查中按钮显示状态，发现新版本自动弹出更新窗口，已是最新或网络异常时弹出提示。

## [0.3.1] - 2026-08-22

### 新增

- 新增 Grok、MiniMax Coding Plan、Claude Code、OpenAI Codex、Gemini CLI 的内置额度适配。
- 支持读取 Grok、Claude、Codex、Gemini 本机 CLI 的登录状态查询订阅额度，不需要在 Quota Desk 中重复填写凭据。
- 支持从本机 `~/.cc-switch/cc-switch.db` 导入 cc-switch 账号，可选择可识别的服务商，重复 API Key 自动去重，扫描过程中凭据只保留在主进程。
- 新增 New API 站点一键模板，包含接口地址、`accessToken` 和 `userId` 变量。
- 补齐内置服务商的官网入口和 Logo。
- 桌面浮窗支持 80%–300% 等比缩放，预览与实际缩放比例一致。

### 改进

- 优化服务商凭据过期或缺失时的错误提示，提供刷新或重新登录指引。
- 空状态页面增加直接导入 cc-switch 的入口，首次配置更顺畅。
- 更新弹窗将 Release HTML 说明转换为纯文本，并过滤自动生成的对比链接尾部。
- 增加 Grok 计费解析、cc-switch 凭据提取与服务商匹配、MiniMax 额度解析测试。
- 重写 README，补充产品定位、支持范围、安装说明，以及固定展示的亮色 / 暗色截图对照。

### 修复

- 修复开发模式下将 CommonJS 模板当作 ES Module 导入导致的白屏。
- 修复历史配置中的服务商 Logo 引用，并自动迁移到内置 Logo。
